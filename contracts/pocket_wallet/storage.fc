(int, slice, slice, cell) load_storage() inline {
    slice ds = get_data().begin_parse();
    return (ds~load_coins(), ds~load_msg_addr(), ds~load_msg_addr(), ds~load_ref(), ds~load_coins());
}

() save_storage(int ton_balance, slice owner_address, slice jetton_master_address, cell jetton_wallet_code, int usdt_balance) impure inline {
    set_data(pack_pocket_wallet_data(ton_balance, owner_address, jetton_master_address, jetton_wallet_code, usdt_balance));
}